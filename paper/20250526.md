# [ONoCGeneral] 当前实现完整说明

> 生成日期: 2026-05-26
> 对应配置: `examples/task_driven/omnetpp.ini` → `[ONoCGeneral]`
> 继承基准: `[General]`（纯电基线，TaskMesh + XYOPCalc）

---

## 一、总体描述

**完整的 task-driven 光电混合 NoC 仿真**：电层控制面握手 → 光层数据直传 → 可重构波长分配 → 设备级光链路预算 → RC 热模型 → DVFS 节流。

```
控制面（电）:  setup_req → 电 router → setup_ack
数据面（光）:  data flit → sendDirect → 光波导一跳直达
```

---

## 二、仿真配置参数

### 基本参数

| 参数 | 值 | 说明 |
|---|---|---|
| `network` | `hnocs.onoc.topologies.ONoCMesh` | 光 NoC Mesh 拓扑 |
| `rows × columns` | 4×4 | 16 节点 |
| `sim-time-limit` | 20ms | 仿真时长 |
| `csvFile` | `tasks_mpeg4_static.csv` | 任务图定义文件 |
| `numVCs` | 4 | 虚通道数 |
| `flitSize` | 16B | 微片大小 |

### 模块类型

| 模块 | 类型 | 说明 |
|---|---|---|
| `coreType` | `TaskPE` | 任务驱动处理单元 |
| `routerType` | `hnocs.routers.hier.Router` | 电路由器 |
| `OPCalcType` | `ReconfigurableOPCalc` | **可重构光路由计算**（区别于静态 XYOPCalc） |
| `VCCalcType` | `FLUVCCalc` | 空闲 VC 优先分配 |
| `schedType` | `SchedSync` | 同步调度器 |

### 光层参数

| 参数 | 值 | 说明 |
|---|---|---|
| `enableSetupHandshake` | true | 电层握手协商光路 |
| `enableOpticalBypass` | true | 光旁路直传数据 |
| `defaultOpticalWavelengths` | 2 | 每条光路默认波长数 |
| `maxOpticalWavelengths` | 8 | 最大可用波长数（4×4 Mesh 双向 bisection=4，8λ 足够覆盖典型无阻塞通信） |
| `numOpticalSpatialChannels` | 2 | 空间通道数（SDM） |
| `opticalWavelengthBitrate` | 256 Gbps | 每波长数据率 |
| `opticalBasePropagationDelay` | 0.5 ns | 光传播基础延迟 |
| `opticalPerHopDelay` | 0.1 ns | 每跳额外延迟 |
| `opticalBurstSize` | 1 | 每次光传输 burst 大小 |
| `opticalWavelengthStrategy` | `"lowest"` | 最低空闲波长优先 |
| `opticalEnableThermalEffects` | **false** | 光热效应关闭 |

### 光层物理（链路预算）

| 参数 | 值 |
|---|---|
| `opticalLaunchPower_dBm` | 0.0 dBm |
| `opticalReceiverSensitivity_dBm` | -18.0 dBm |
| `opticalSourceModulatorLoss_dB` | 2.0 dB |
| `opticalHopInsertionLoss_dB` | 0.5 dB |
| `opticalHopCrosstalkLoss_dB` | 0.1 dB |
| `opticalReceiverDemodulatorLoss_dB` | 2.0 dB |

### PE 功耗模型

| 参数 | 值 |
|---|---|
| `powerIdle` | 0.5 W |
| `powerCompute` | 2.0 W |
| `powerSendPerFlit` | 1e-10 J (0.1 nJ) |
| `powerRecvPerFlit` | 5e-11 J (0.05 nJ) |
| `opticalModulatorEnergyPerFlit` | 2e-12 J (2 pJ) |
| `opticalReceiverEnergyPerFlit` | 1e-12 J (1 pJ) |
| `energyWindow` | 100 ns |
| `enableEnergyWindow` | true |
| `enablePowerTrace` | true |

### RC 热模型

| 参数 | 值 | 单位 |
|---|---|---|
| `RconvPE` | 8 | K/W |
| `RconvRouter` | 10 | K/W |
| `RlateralPE` | 15 | K/W |
| `RlateralRouter` | 15 | K/W |
| `Rpe2router` | 3 | K/W |
| `Cpe` | 1e-6 | J/K |
| `Crouter` | 2e-7 | J/K |
| `Tambient` | 318.15 (45°C) | K |
| `Tthrottle` | 320.0 (47°C) | K |
| `throttleBeta` | 0.05 | 1/K |

### Router 功耗

| 参数 | 值 |
|---|---|
| `eBufferWrite` | 1e-12 J |
| `eBufferRead` | 1e-12 J |
| `eCrossbar` | 0.5e-12 J |
| `pLeak` | 1e-3 W |
| `inPort.enableEnergyWindow` | true |
| `inPort.energyWindow` | 100 ns |

---

## 三、波长分配机制

### 分配触发时机

波长分配在 `LogicalTopologyManager::tryAllocateOpticalPathForPacket()` 中完成（`src/onoc/control/LogicalTopologyManager.cc:1481`），由源路由器上的 **ReconfigurableOPCalc** 在第一个数据 flit 到达时触发。

### 完整流程

```
源 PE 完成计算 → 需要发数据给下游
        │
        ▼
① 电层握手 (setup handshake)
   - TaskPE.cc:1010 调用 tryReserveSetupPath(dst)
   - LogicalTopologyManager 在 edgeOccupancy 中预留波长+空间通道
   - 通过电路由器发 SETUP_REQ（2-flit 控制包，taskId=-1）
   - 目的 PE 收到后发 SETUP_ACK 回应
   - 源 PE 收到 ACK，标记 circuitReadyByDst[srcIdx] = 1
   - 超时重试：setupRetryDelay=50ns, setupPendingTimeout=200ns
        │
        ▼
② 数据 flit 触发波长分配
   第一个 NOC_START_FLIT 到达源路由器时 (ReconfigurableOPCalc.cc:312):
   - 若 srcId == routerId 且波长尚未分配
   - → topologyManager->tryAllocateOpticalPathForPacket(src, dst, pktId)
        │
        ▼
③ 核心分配算法 (LogicalTopologyManager.cc:1523-1565)
   a. buildXYPathEdges() — 确定源到目的地的 XY 维序路由边列表
   b. 确定所需波长数（默认 2 个：opticalRequiredWavelengths）
   c. 遍历空间通道 (0 → numOpticalSpatialChannels-1):
      - isWavelengthFreeOnPath(): 检查每个波长是否在所有边上空闲
      - 收集所有空闲波长索引
      - 策略 "lowest": 直接取最小的 required 个空闲波长
      - 策略 "thermal": 评测候选组合的光预算，选调谐功率最小的（当前未启用）
      - 若空闲波长不足 → 尝试下一个空间通道
   d. 占用波长: edgeOccupancy[spatial][wlIdx] = pktId（所有边）
   e. 存储分配: opticalPacketAllocations[pktId] = {src, dst, spatial, wls, edges}
   f. 预计算设备级光链路预算并缓存
   g. 若所有空间通道都不够 → insufficientResources = true → 丢包
        │
        ▼
④ 光层数据直传
   TaskPE.cc:659 sendOpticalFlitFromQ()
   - 从 opticalDataQ 取 flit
   - sendDirect(flit, propDelay, txDuration, targetMod->opticalIn)
   - 数据 flit 通过 OMNeT++ sendDirect 在光链路上端到端直传
   - 完全绕过电路由器的 store-and-forward
        │
        ▼
⑤ 释放
   NOC_END_FLIT 发出后:
   - releaseOpticalPathForPacket(pktId)
   - 清空所有路径边上的 edgeOccupancy 条目
   - 从 opticalPacketAllocations 中移除
   - 若使用了 SOA/调谐功率，从 ThermalModel 中减去
   - circuitReadyByDst 清零
```

### 关键数据结构

```
opticalEdgeOccupancy:
  map<edgeKey, vector<vector<int>>>
  // 每条无向边 → [numSpatialChannels][maxWavelengths] 矩阵
  // 非零值 = 占用该 (spatial, wavelength) 的 pktId
  // 零 = 空闲

opticalPacketAllocations:
  map<pktId, OpticalPacketAllocation>
  // 每条活跃光路的完整信息
```

### 当前策略

- **`"lowest"`**: 遍历空闲波长表，选索引最小的 N 个。快速、确定性。
- **无热感知**: `opticalEnableThermalEffects = false` → 不评估温度对 MRR 谐振的影响，不计算调谐功率。

---

## 四、功耗计算

### PE 功耗（TaskPE.cc）

#### 静态功耗（泄漏）
```
leakageFactor = exp((T_pe - Tambient) / 15.0)
// 温度每升高约 10°C，泄漏约翻倍

currentPower_idle = powerIdle × leakageFactor       // 空闲态
currentPower_busy = (powerCompute-powerIdle) + powerIdle × leakageFactor  // 计算态

windowStaticEnergyJ += currentPower × dt
```

#### 动态功耗（开关）
```
windowDynamicEnergyJ = 
    N_send × powerSendPerFlit       // 电发送: 0.1 nJ/flit
  + N_recv × powerRecvPerFlit       // 电接收: 0.05 nJ/flit
  + N_opt_send × opticalModulatorEnergyPerFlit   // 光调制器: 2 pJ/flit
  + N_opt_recv × opticalReceiverEnergyPerFlit    // 光接收器: 1 pJ/flit
```

#### 能量窗口汇总
```
windowEnergyJ = windowStaticEnergyJ + windowDynamicEnergyJ
windowAvgPower = windowEnergyJ / energyWindow     // energyWindow = 100ns

→ submitPEPower(peId, now, windowAvgPower)  → 送入 ThermalModel
```

### Router 功耗（InPortSync.cc）

```
windowDynamicEnergyJ =
    N_bufferWrite × eBufferWrite     // 1 pJ/次
  + N_bufferRead  × eBufferRead      // 1 pJ/次
  + N_crossbar    × eCrossbar        // 0.5 pJ/次

windowStaticEnergyJ = pLeak × leakageFactor × energyWindow
// leakageFactor 与 PE 相同: exp((T_router - Tambient) / 15.0)

→ 由 port[0] 汇总所有端口 → submitRouterPower(routerId, now, avgPower)
```

### 光器件功耗

因 `opticalEnableThermalEffects = false`，以下两项**均为 0**：

| 项目 | 条件 | 说明 |
|---|---|---|
| MRR 热调谐功率 | `opticalEnableThermalEffects=true` 时 | 补偿温度引起的谐振波长漂移所需的加热功率 |
| SOA 泵浦功率 | SOA 使能时 | 每 SOA 约 15 mW 电泵浦功率 |

### 当前未建模的功耗

- **激光器电功率**: 仅有光域 `launchPower_dBm`，无电→光转换效率（WPE）模型
- **光调制器/接收器驱动电路功耗**: 仅有每 flit 能量，无静态偏置功耗

---

## 五、热仿真（RC 热模型）

### 模型结构（ThermalTrace.cc:261 `updateTemperature`）

**两层二维 RC 热网络**，PE 层和 Router 层各 `rows×cols` 个节点。每个节点：

```
dT_pe[i]/dt × Cpe =
    pePower[i]                                    ← 热源
  - (T_pe[i] - Tambient) / RconvPE                 ← 垂直对流到环境
  - (T_pe[i] - T_router[i]) / Rpe2router           ← 垂直到本地 Router
  - Σ(T_pe[i] - T_pe[neighbor]) / RlateralPE       ← 横向传导到邻居 PE

dT_router[i]/dt × Crouter =
    routerPower[i] + routerOpticalPower[i]          ← 电功率 + 光器件功率
  - (T_router[i] - Tambient) / RconvRouter
  - (T_router[i] - T_pe[i]) / Rpe2router
  - Σ(T_router[i] - T_router[neighbor]) / RlateralRouter
```

### 求解方法

**显式 Euler 法**，每个 `energyWindow` (100ns) 推进一次：
```
T_new = T_old + (heatIn / C) × dt
```

### 触发机制（`tryFlush`）

```
每个 energyWindow 结束时:
  1. 各 PE 调用 submitPEPower(peId, t, avgPower)
  2. 各 Router 调用 submitRouterPower(routerId, t, avgPower)
  3. 当所有节点都提交了本窗口功率 → tryFlush() 触发
  4. routerPower[i] += routerOpticalPower[i]（合并光器件功率）
  5. updateTemperature(dt) → Euler 推进
  6. 写 HotSpot 格式热 trace 文件
  7. 为下一个窗口重置 ready 标志
```

### 温度反馈路径

```
温度 → DVFS 节流:  computeTime ×= (1 + 0.05 × (T-320))  当 T > 47°C
温度 → 泄漏功耗:    currentPower ×= exp((T-318.15)/15)
```

### 光学热效应状态

`opticalEnableThermalEffects = false` → 以下**全部关闭**：
- MRR 谐振波长温漂（thermo-optic coefficient）
- 温漂引起的额外插入损耗
- 补偿温漂所需的调谐功率
- SOA 增益温度退化
- 波导温度相关额外损耗

---

## 六、光损耗计算

### 简单线性模型（后备）

```
totalLoss_dB = 调制器损耗(2dB)
             + hopCount × (hopInsertionLoss(0.5dB) + hopCrosstalkLoss(0.1dB))
             + 解调器损耗(2dB)

receivedPower_dBm = launchPower_dBm(0) - totalLoss_dB
meetsSensitivity = receivedPower_dBm >= receiverSensitivity_dBm(-18)
signalMargin_dB = receivedPower_dBm - receiverSensitivity_dBm
```

### 设备级精细模型（实际使用）

`OpticalDeviceModel::computeDeviceLevelBudget()` (`src/onoc/common/OpticalDeviceModel.cc:387`) 为每条光路构建完整物理设备链：

```
激光器 → 波导段 → 调制器微环链(环通过×N + 环下载)
  → 路由器内部(端口转向: 环通过×M + 环下载 + 波导弯曲)
  → 跨路由器波导段 → SOA
  → ... (重复 XY 维序路由每一跳)
  → 解调器微环链(环通过×K + 环下载) → 光电探测器
```

每段累加：

| 损耗来源 | 建模方式 |
|---|---|
| 调制器/解调器 | 环通过损耗 0.05 dB/环 + 环下载 1.0 dB |
| 路由器内部 | 环通过次数依赖端口转向（波长相关公式），每跳 1 次下载 + 弯曲损耗 0.01 dB/次 |
| 波导传播 | 1.5 dB/cm × 段长 |
| SOA | 增益 13 dB（负损耗）+ ASE 噪声（NF=7 dB, B=30 GHz） |
| 相邻串扰 | -20 dB 泄漏到相邻波长通道 |
| 非相邻串扰 | -40 dB 泄漏到非相邻通道 |
| 解调器串扰 | λ_{i-1} 的通过端口泄漏到 λ_i 的下载端口 |

### SNR 与 BER 计算

```
rxPower_mW = 10^(rxPower_dBm/10)
totalNoise_mW = 热噪声(mW) + ASE噪声(mW) + 串扰噪声(mW)
SNR_linear = rxPower_mW / totalNoise_mW
SNR_dB = 10 × log10(SNR_linear)

PAM4: BER = 0.75 × erfc(√(SNR_linear / 10))
OOK:  BER = 0.5  × erfc(√(SNR_linear / 2))
```

### 链路预算的角色

**预算不阻止建链。** 即使 BER 超标或 receivedPower < sensitivity，光路仍会建立。预算数据仅用于：
- 统计记录（`opticalPathTotalLossDb`, `opticalPathReceivedPowerDbm`, `opticalPathMarginDb`）
- 可选的重路由触发（`enableBudgetBasedRerouting`，当前默认 false）

---

## 七、功能矩阵总结

| 功能 | 状态 | 关键参数/模块 |
|---|---|---|
| Task-driven 任务图仿真 | ON | `csvFile` |
| 电层 setup 握手 | ON | `enableSetupHandshake = true` |
| 光层 bypass 数据直传 | ON | `enableOpticalBypass = true` |
| 可重构波长分配 | ON | `ReconfigurableOPCalc`, 策略 `"lowest"` |
| PE/Router 能耗核算 | ON | 静态+动态，含温度修正泄漏 |
| RC 热仿真 | ON | Euler 法，两层二维网格 |
| DVFS 节流 | ON | T>47°C 启动，5%/°C |
| 设备级光链路预算 | ON | SNR + BER，每波长独立 |
| 光热调谐 | **OFF** | `opticalEnableThermalEffects = false` |
| 热感知波长选择 | **OFF** | 依赖 `opticalEnableThermalEffects` |
| 预算驱动重路由 | **OFF** | `enableBudgetBasedRerouting` 默认 false |
| 激光器电功耗模型 | 未实现 | 仅光域 launchPower |

---

## 八、可切换的任务图配置

通过 `extends = ONoCGeneral` 继承全部光电参数，仅覆写 csvFile：

| Configuration | CSV 文件 |
|---|---|
| `[ONoC_MPEG4]` | `tasks_mpeg4_static.csv` |
| `[ONoC_GEMM]` | `tasks_gemm_static.csv` |
| `[ONoC_VOPD]` | `tasks_vopd_static.csv` |
| `[ONoC_Optic]` | `optic_static.csv` |

---

## 九、关键源文件索引

| 组件 | 文件 |
|---|---|
| 波长分配（核心算法） | `src/onoc/control/LogicalTopologyManager.cc` |
| 波长分配（路由触发） | `src/onoc/routing/ReconfigurableOPCalc.cc` |
| PE 能耗 + 握手 | `src/cores/task/TaskPE.cc` |
| Router 能耗 | `src/routers/hier/inPort/InPortSync.cc` |
| RC 热模型 | `src/thermal/ThermalTrace.cc` `.h` |
| 设备级光链路预算 | `src/onoc/common/OpticalDeviceModel.cc` `.h` |
| 光路指标结构 | `src/onoc/common/OpticalPathMetrics.h` |
| 控制面事件编码 | `src/onoc/common/ControlPlaneEvents.h` |
| 光电路控制器 | `src/onoc/optical/OpticalCircuitController.cc` |
| 顶层拓扑 NED | `src/onoc/topologies/ONoCMesh.ned` |
| 逻辑拓扑管理器 NED | `src/onoc/control/LogicalTopologyManager.ned` |
| 配置文件 | `examples/task_driven/omnetpp.ini` |
